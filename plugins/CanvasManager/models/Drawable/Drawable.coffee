class Drawable extends Model
    # to be redefined
    draw: ( info ) ->

    update_min_max: ( x_min, x_max ) ->
 
    get_movable_entities: ( res, info, phase ) ->
    
    z_index: ->
        0
        
    sub_canvas_items: ->
        []
